import pandas as pd

# Approach 1
# def modifySalaryColumn(employees: pd.DataFrame) -> pd.DataFrame:
#     employees["salary"] *= 2
#     return employees
# ------------------------------------------------------------------
# Approach 2
def modifySalaryColumn(employees: pd.DataFrame) -> pd.DataFrame:
    employees.salary *= 2
    return employees